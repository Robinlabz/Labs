{
  "name": "robinlabs-indexer",
  "version": "1.0.0",
  "private": true,
  "description": "Robin Labs Pad indexer + JSON API. Reads the chain, serves a fast browse feed, trades, volume and graduation state so the static pad never has to fan out dozens of RPC calls per page.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "index": "node src/server.js --no-api",
    "api": "node src/server.js --no-index",
    "backfill": "node src/server.js --once --no-api"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "ethers": "^6.13.5"
  }
}
